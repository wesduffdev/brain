[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: exercises a live external dependency (e.g. Postgres); skipped when unreachable
    kafka: exercises a live Kafka broker (publish->consume, dedupe, DLQ); skipped when the broker/KAFKA_BOOTSTRAP_SERVERS is absent
